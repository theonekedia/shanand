<%= form_for(@industry_detail) do |f| %>
  <% if @industry_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@industry_detail.errors.count, "error") %> prohibited this industry_detail from being saved:</h2>

      <ul>
      <% @industry_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
