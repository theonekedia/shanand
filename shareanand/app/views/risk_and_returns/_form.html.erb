<%= form_for(@risk_and_return) do |f| %>
  <% if @risk_and_return.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@risk_and_return.errors.count, "error") %> prohibited this risk_and_return from being saved:</h2>

      <ul>
      <% @risk_and_return.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
